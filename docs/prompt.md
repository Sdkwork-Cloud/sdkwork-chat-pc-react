修复中的错误问题，确保错误得到解决
当前不要做迁移操作，而是修复错误，以下是详细的规则：
逐个/src/modules/下的模块进行迁移
执行基础模块的系统性分包封装工作，将现有基础模块按照以下详细规范重构为独立package。实施过程需遵循严谨的迁移流程，确保架构转换的完整性和代码质量。

一、分包实施步骤：
1. 前期准备阶段
   - 使用命令行工具批量创建符合规范的分包基础结构，包括必要的目录层级、配置文件和基础代码框架
   - 制定详细的迁移checklist，涵盖文件迁移、依赖更新、测试验证等关键节点，明确每个节点的责任人、完成标准和时间节点
   - 准备必要的迁移辅助工具和脚本，提高迁移效率，确保迁移过程可重复、可追溯
   - 建立代码规范检查机制，集成ESLint、Prettier等工具，确保迁移后代码符合项目编码标准

2. 核心基础包优先开发
   - 优先重构三个核心基础包，作为所有业务模块的依赖基础：
     * sdkwork-react-commons：通用组件包，包含项目通用UI组件和工具函数
     * sdkwork-react-i18n：国际化功能封装包，提供统一的国际化解决方案
     * sdkwork-react-core：核心功能模块包，包含路由、状态管理等核心功能
   - 为每个核心包编写完整API文档、使用示例和单元测试，测试覆盖率需达到80%以上
   - 采用语义化版本控制核心包版本号，确保向后兼容性，版本号格式遵循MAJOR.MINOR.PATCH规范
   - 完成核心包单元测试并验证功能，发布基础版本至私有npm仓库

3. 业务模块迁移实施
   - 按优先级顺序对/src/modules下的业务模块进行逐个迁移，制定详细的模块迁移顺序表
   - 每个业务模块迁移严格遵循以下流程：
     1. 将模块迁移至顶级目录，采用"${package_prefix}-react-xxx"命名格式（"xxx"对应原modules下的目录名称）
     2. 确保100%组件和页面文件完整迁移，无文件遗漏，建立文件清单进行核对
     3. 更新模块依赖关系，显式依赖三个核心基础包，明确指定依赖版本范围
     4. 移除模块内重复实现的功能，统一使用核心包组件，消除代码冗余
     5. 优化代码结构，移除冗余代码，提高复用率，重构复杂逻辑提升可读性
     6. 为模块添加完整的i18n国际化配置，确保所有用户可见文本均支持国际化
     7. 配置完整的package.json文件，明确入口文件、依赖项、版本信息、作者、许可证等元数据
     8. 实现模块双重功能：作为独立Web应用支持单独启动；作为package供外部导入使用，需配置相应的构建脚本
     9. 每个模块迁移前，先分析清楚模块间的依赖关系，从最基础的模块开始逐个迁移，基础模块优先级高一些，建立模块依赖关系图谱
     10.在迁移前先把/src/modules/下的所有文件备份到/bak/modules/目录下。确保迁移所有文件到对应"${package_prefix}-react-xxx"中。
   - 逐个文件进行修改，禁止采用批量脚本替换方式，确保代码修改的准确性和可追溯性
4. 依赖关系调整
   - 修改所有迁移后模块的依赖导入关系，确保路径正确无误
   - 建立分包间依赖管理机制，实施依赖版本控制策略，避免版本冲突
   - 确保分包间依赖正确配置，支持多分包install使用，测试不同分包组合的安装和运行情况

5. 清理与验证
   - 确认迁移和依赖调整无误后，删除/src/modules/下的对应原始目录
   - 对迁移后的每个模块进行全面测试：
     1. 执行单元测试和集成测试，确保测试覆盖率不低于70%
     2. 进行功能验证确认无误，编写测试用例并执行
     3. 通过代码审查，由至少一名团队成员进行代码审核
     4. 完成相关文档更新，包括API文档、使用说明和变更日志
     5. 在修改代码前要将/src/modules/xxxx对应的目录移动到/bak/目录中，方便恢复和检查错误，备份目录需包含时间戳

二、质量控制要求：
1. 国际化检查：迁移过程中需检查所有文件，确保不存在未国际化的文本，按i18n规范处理所有页面文本，建立国际化检查清单
2. 命名规范审核：建立命名审核机制，确保所有package名称严格符合"${package_prefix}-react-xxx"格式，编写命名检查脚本进行自动化验证
3. 每日构建测试：实施每日构建和测试机制，及时发现解决迁移问题，建立CI/CD流水线确保构建和测试自动化
4. 进度报告：定期提交迁移进度报告，包括已完成模块、当前进度、遇到的问题和解决方案，每日更新进度情况
5. 当${package_prefix}-react-xxx中存在报错的时候，不要进行删除，而是确认要修复。请处理。
5. 要完整把/src/modules/xxx的所有文件迁移，迁移完比后，验证"${package_prefix}-react-xxx"的正确性，确保正确后，删除对应的/src/modules/xxx
 