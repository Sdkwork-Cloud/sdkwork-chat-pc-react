/**
 * Re-export component contracts from the canonical contracts package.
 */

export type {
  SlotComponentProps,
  AsyncStateProps,
  FeaturePageProps,
  ListViewProps,
} from "@sdkwork/openchat-pc-contracts";
